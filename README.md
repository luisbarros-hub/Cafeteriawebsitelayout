
  # Cafeteria Website Layout

  This is a code bundle for Cafeteria Website Layout. The original project is available at https://www.figma.com/design/UTheMb5KpfAmNEpOkOqLtS/Cafeteria-Website-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  